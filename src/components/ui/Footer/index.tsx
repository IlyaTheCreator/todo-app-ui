import React from "react";

import classes from "./Footer.module.css";

const Footer: React.FC = () => {
  return <footer className={classes.footer}>I am a footer</footer>;
};

export default Footer;
